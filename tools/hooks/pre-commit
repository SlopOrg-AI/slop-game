#!/bin/sh
# Guard rails, D5.39. Enable per clone: git config core.hooksPath tools/hooks
if command -v python3 >/dev/null 2>&1; then PY=python3; else PY=python; fi
exec "$PY" "$(git rev-parse --show-toplevel)/tools/hooks/checks.py" pre-commit
