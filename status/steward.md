# steward

- **state** · `steward-260921-a`, first Steward session (Claude Code, clone `shinobi-cos`). Read `proposals/2026-09-21-clean-slate.md`, `proposals/2026-09-21-cos-handoff-b.md`, `design/decisions.md` §Session 7. **The §11 migration has not landed.** Present: `PROTOCOL.md`, `roles/builder.md`, `queue/_DOORBELL.md`. Absent: `roles/designer.md`, `roles/steward.md`, `status/` render, `queue/_TEMPLATE.md`, the launcher; `leads/` `inbox/` `bridge/` unarchived; `sweep.py` and `watch.py` still live; `AGENTS.md` 7.5 KB against a 3 KB budget.
- **next** · nothing, until §11 lands. Then, in order: audit `PROTOCOL.md` and the three charters against the approved design, one line per discrepancy here; seed `queue/` with the three game questions that block the duel demo (card model / C23 · the reserve object · the scope of `design/02-ontology.md`).
- **not finished** · queue unseeded, doorbell never run — both wait on §11. No audit written: auditing a half-migrated tree produces findings that the migration itself would erase.
- **landed this session** · D260921.1-P and D260921.2-P were ruled on 2026-09-21 but had never reached git — the two rows that bind every role were invisible in the repo. Committed here, with `clean-slate.md` §12 (the naming and messaging grammar, Builder implements), the FINAL `cos-handoff-b.md`, and session `a5`'s implementation review. Doorbell steps 4–5 reworded — Steward steers what it asks, Builder owns the mechanism (`PROTOCOL.md` §1).
- **asks** · `A260921.1` → **builder** · run the §11 migration checklist, and read `proposals/2026-09-21-clean-slate-implementation-review.md` first — its §3 (make the worktrees by hand; the launcher comes *after* the hard stop) and §4 (four revertible commits, delete nothing until the new world is proven) change how §11 should be run. Create the doorbell from a Claude Code cloud session; it is not running. · blocks: every Steward duty, the owner's queue, and the hard stop on `design/02-ontology.md`.
