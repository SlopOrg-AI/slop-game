# Designer — state · next · asks

**Seat:** unclaimed · **Updated:** 2026-09-21, by `builder-260921-c` during the board collapse. The Designer owns this file from its first session; Builder created it because `D260921.1-P` specifies a `status/<role>.md` per role and this one had never been written.

**state** · No Designer session has run under the clean-slate framework. Everything below was carried off `STATUS.md` when it collapsed to one page. These are live items with no other home, and several had their only pointer inside `leads/`, which is due to be archived.

**next** · C23 is the oldest, owed since 2026-09-20.

## Carried from the old board — not ruled, not closed

| # | Item | Came from |
|---|---|---|
| 1 | **C23 — cards as three decks drawn by choice.** Owner verbatim in `inbox/2026-09-20-three-deck-draw.md`. Handed to Systems through `leads/chief-of-staff.md` §For Systems and never actioned. The implementation review §5 flags this one by name: **archiving `leads/` destroys its only live pointer.** | `STATUS.md` §For Systems |
| 2 | **`D5.24` is asserted DECIDED in six files and does not exist.** `D5.47-AES` absorbed it and says it gets no number of its own. Re-point when the rule text lands: `00-steer` §6, `11-initiative.md` (its status table still asserts it), `14-scenes-conditions.md`, `leads/systems.md`, `leads/direction/level.md`, and the old board. | §Housekeeping + §Conflicts #1 |
| 3 | **Four rows are logged, not closed** — `D5.46-ES`, `D5.47-AES`, `D5.50-CES`, `D5.51-CES`. Their rule text exists only in `proposals/2026-09-20-initiative-and-earmarks.md`; `10`/`11`/`12` are stubs, and `D5.41-EP` P2 forbids closing a D-number into a stub. **Do not archive that proposal** — it is the only copy of the text. | `decisions.md` §Logged, not closed |
| 4 | **Two tag corrections**, corrigible in place without renumbering (`D5.43-P` cl.4). `D5.33-EP` — escape as a fifth exit condition is a game rule, tagged from a cell that read "Tech + Production"; almost certainly wants **S**. `D5.2-PS` — carries a Production clause and an Engine clause, and the log's own header says a row carrying two is really two rows. | §For Systems |
| 5 | **`design/40-production.md` §5** — the M1/M2 definitions were held pending Q1/Q2. Both are now ruled (`D5.46-ES`, `D5.47-AES`). | §Housekeeping |
| 6 | **`data/rules.json` is stale in two places** — its `_note`s cite v1 open numbers, and `insight.reveal_timing` references the Arm step, which was removed. | §Housekeeping |
| 7 | **`sys.7` and `sys.8`** were recorded in `leads/systems.md` and never promoted to D-numbers. `D260921.1-P` retired local refs, so nothing will promote them now: they are either already superseded or they are unlogged rulings. Answers `A260921.9 → designer`. | §Pending promotion |

**Dropped deliberately, recorded so the choice is visible rather than silent:** the `Acts on it` column item (that column was deleted with `D5.43-P`) · the sweep table A.1 row (`tools/sweep.py` no longer exists) · the Project mirror re-sync (`D260921.1-P` retires the mirror — "the Project reads the repo, or nothing") · D1–D4 compression (the old board itself marked it "no longer a deadline"; the text is in `sources/v1/docs/combat-scene-decisions.md`).
